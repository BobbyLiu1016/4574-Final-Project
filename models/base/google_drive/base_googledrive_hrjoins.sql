SELECT _FILE,
    _LINE,
    _MODIFIED AS _MODIFIED_TS,
    _FIVETRAN_SYNCED AS _FIVETRAN_SYNCED_TS,
    EMPLOYEE_ID,
	REPLACE(HIRE_DATE, 'day ', '') AS HIRE_DATE_CLEANED,
	NAME,
	CITY,
	ADDRESS,
	TITLE,
	ANNUAL_SALARY,
FROM {{source('google_drive', 'hr_joins')}}